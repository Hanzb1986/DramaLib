# 2026-07-08

## 📋 Cron 排期规则（Hans 统一规范，受信记录）

**来源：** Nova 转发的 Hans 统一指令，全 Agent 遵守

### 新增/修改 cron 前置检查
1. `cron list` 拉取全部已启用 cron（Nova/Ellie/Drama/Lina 所有）
2. 比对目标 expr 是否与现有 cron 在同一小时+同一分钟
3. 有冲突 → 按5分钟间隔顺延（:00 → :05 → :10 → :15…）
4. Hans 的 cron 优先占整点 :00
5. 内部巡检类 cron 用偏移分钟或小 staggerMs（15~30s）

### 当前已占用时刻
- 07:55 — 任务节点巡检
- 08:00 — daily-full-report 🔴
- 10:00 / 12:00 / 14:00 / 16:00 — daily-check 🟡
- 18:00 — daily-summary 🟡
- HH:00+0~15s 偶数小时 — skill-snapshot-scan
- HH:00+0~30s 每4小时 — agent-heartbeat
- 周一 07:00 — 行业竞品汇总

### 纯机械操作
- 纯 shell 操作（如 git push）不走 agentTurn 调大模型，只跑脚本
- ⚠️ 高频模型调用禁令：任何纯机械操作禁止使用 agentTurn 调模型

## 📋 更新：Cron 统一管理机制（Nova 排期制）

**时间：** 2026-07-08，即时生效
**来源：** Nova 转发的 Hans 统一指令

### 三条核心规则
1. **申请制** — 所有 Agent 的定时任务由 Nova 统一排期管理，不再自己创建/修改 cron，需先向 Nova 申请时段
2. **10分钟间隔** — cron 排期间隔调整为 10分钟（区别于早期版本的 5分钟）
3. **高频模型调用禁令** — 纯机械操作（如 git push 等）禁止使用 agentTurn 调模型，只跑 shell 脚本

### Drama 现状
- `drama-morning-production` — `5 10 * * *`（10:05）✅ 已注册无冲突
- `drama-evening-production` — `0 20 * * *`（20:00）✅ 已注册无冲突

### Drama 现有 cron 检查结果（2026-07-08）
- `drama-morning-production` — `0 10 * * *`（10:00）❌ 与 daily-check 冲突 → 需改为 `5 10 * * *`（10:05）
- `drama-evening-production` — `0 20 * * *`（20:00）✅ 无冲突

### 执行修正
- `drama-morning-production` expr 已从 `0 10 * * *` 改为 `5 10 * * *`
